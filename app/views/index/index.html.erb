<div id="mainmenu">

<% 
  @team = nil
  @team = Team.get_team(current_user) if current_user != nil
%>

<%= link_to "Find teams",  teams_path %>
<p class="explain">Search for teams here. Join a team, check out the competition, or just see what these teams are all up to and what the fuss is all about!</p>
 
<% if @team %>
<%= link_to @team.name, "/teams/#{@team.id}" %>
<p class="explain">Go directly to your team!</p>
<% else %>
<%= link_to "Get Started - Register", new_team_path %>
<p class="explain">Team captains, this is for you. Sign up here to start a brand new team and start raising money for Ethos! If you already have a team captain, find your team in the "Find Teams" section, and join from there.</p>
<% end %>
 
<%= link_to "Make a donation!", donate_path %>
<p class="explain">Click here to donate to a specific team or to directly to the event.</p>

<% if current_user != nil && current_user.admin? %> 
<%= link_to "Administration", admin_path %>
<% end %>

<div id="teamtop5">
<div class="boxxy">
  <%= image_tag "top5h2.png", :title => 'Top 5 Raisers' %><br />
  <table class="top5">
    <tr>
      <th title="top fundraising teams" scope="col" width="143">teams</th>
      <th title="amount of money raised" scope="col" width="50">$$$</th>
    </tr>
    <% Team.payment_top_teams.each do |team| %>
    <tr class="listview">
      <td><%= team.name %></td>
      <td><%= team.total_donations %></td>
    </tr>
    <% end %>
  </table>
  <table class="top5" style="">
    <tr>
      <th title="top fundraising members" scope="col" width="143">members</th>
      <th title="amount of money raised" scope="col" width="50">$$$</th>
    </tr>
    <% Team.payment_top_users.each do |user| %>
    <tr class="listview">
      <td><%= user.firstname %> <%= user.lastname %></td>
      <td><%= user.payments %></td>
    </tr>
    <% end %>
  </table>
  </div>
</div>
</div>
