<h1>Register</h1>
<p>Already have an account? <%= link_to "Signin", signin_path %> instead.</p>

<% form_for(@user, :url => account_path, :html => { :multipart => true } ) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Register" %>
<% end %>
